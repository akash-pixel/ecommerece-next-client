export enum ROLE {
    USER = 0
}

export const WebRef = {
    TOKEN: "token",
    USER_IDENTITY: "user_identity"
};

export enum FORM {
    ADD = "add",
    EDIT = "edit"
}

export enum TOAST_TYPE {
    SUCCESS = "success",
    ERROR = "error",
    LOADING = "loading"

}

export const ErrorCode = {
    UNAUTHORIZED: 401
}